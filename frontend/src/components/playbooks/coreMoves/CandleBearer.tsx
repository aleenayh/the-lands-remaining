import type { Character } from "../types";

export function CoreMoveCandleBearer({ character }: { character: Character }) {
	return (
		<div>
			<h2>The Waxen Order Keeps the Flame…</h2>
			<p>This is the core move for the Candle Bearer playbook.</p>
		</div>
	);
}
